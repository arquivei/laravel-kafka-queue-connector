version: "2.0"
services:

    php-fpm:
      build: docker/php-fpm
      container_name: docker-kafka-connector-php-fpm
      working_dir: /application
      volumes:
        - .:/application
        - ./docker/php-fpm/php-ini-overrides.ini:/etc/php/7.2/fpm/conf.d/99-overrides.ini
